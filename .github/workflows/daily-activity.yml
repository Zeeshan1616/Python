name: Daily Activity

on:
  schedule:
    - cron: '0 6 * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  empty-commit:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          ref: main

      - name: Make 10 empty commits
        run: |
          git config user.name "GitHub Actions Bot"
          git config user.email "actions@github.com"
          for i in $(seq 1 10); do
            git commit --allow-empty -m "chore: daily activity check-in $i"
          done
          git push origin HEAD:main
